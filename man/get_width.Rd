% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/width.R
\name{get_width}
\alias{get_width}
\title{Standardise \code{ggplot2} geom width}
\usage{
get_width(
  ...,
  n = NULL,
  n_dodge = NULL,
  orientation = c("x", "y"),
  value = NULL,
  panel_widths = NULL,
  panel_heights = NULL
)
}
\arguments{
\item{...}{Must be empty. Forces all other arguments to be named and allows
trailing commas.}

\item{n}{Number of categories in the orientation aesthetic (that is, \code{"x"} or
\code{"y"}). For faceted plots, use the maximum \code{n} across facets.}

\item{n_dodge}{Number of dodge categories. Intended for use with
\code{position_dodge(preserve = "single")}.}

\item{orientation}{Orientation: \code{"x"} for vertical geoms, \code{"y"} for horizontal geoms.}

\item{value}{Numeric scaling factor controlling apparent width. A value of
\code{1} is the default. Increase to make a wider appearance; decrease to make a
thinner appearance. If \code{NULL}, uses the value set by \code{\link[=update_equiwidth]{update_equiwidth()}},
falling back to \code{1}.}

\item{panel_widths}{A \code{grid::unit} object specifying panel widths. If \code{NULL},
uses the value from the current theme.}

\item{panel_heights}{A \code{grid::unit} object specifying panel heights. If \code{NULL},
uses the value from the current theme.}
}
\value{
A single numeric width suitable for the \code{width} argument of geoms such
as \code{\link[ggplot2:geom_bar]{ggplot2::geom_bar()}} or \code{\link[ggplot2:geom_col]{ggplot2::geom_col()}}.
}
\description{
Standardise the width in \code{ggplot2} geoms so that widths appear visually
consistent across plots with different numbers of categories, panel dimensions,
and orientations.

This can be used in geoms such as \code{\link[ggplot2:geom_bar]{ggplot2::geom_bar()}},
\code{\link[ggplot2:geom_col]{ggplot2::geom_col()}}, \code{\link[ggplot2:geom_boxplot]{ggplot2::geom_boxplot()}},
and \code{\link[ggplot2:geom_errorbar]{ggplot2::geom_errorbar()}}.

The relevant panel dimension must be an absolute physical unit (for example
\code{grid::unit(..., "mm")}), either supplied directly or set in the current theme.
Relative units are not supported.
}
